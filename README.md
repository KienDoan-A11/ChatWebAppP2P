# Cấu trúc
Final_dieuchinh
 index.html          # Giao diện người dùng
 style.css           # Styles cho UI
 script.js           # Logic xử lý frontend
 server.js           # Server signaling
 package.json        # Dependencies

# Cách sử dụng
1. npm install (install thư viện cần thiết)
2. node server.js (khởi tạo server)
3. chạy live server ở file index.html để mở giao diện và sử dụng
